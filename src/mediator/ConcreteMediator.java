package mediator;

import flipper_elements.FlipperItem;

public class ConcreteMediator implements Mediator{
    @Override
    // was passiert falls alle targets von FlipperItem getroffen sind
    public void notify(FlipperItem item) {

    }

}
